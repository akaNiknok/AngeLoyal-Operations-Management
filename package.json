{
  "name": "angeloyal-operations-management",
  "private": true,
  "version": "1.0.0",
  "description": "AngeLoyal Logistics - Operations Management System (Google Apps Script)",
  "scripts": {
    "help": "node scripts/help.js",
    "test": "node --test \"test/**/*.test.js\"",
    "login": "clasp login",
    "use:dev": "node -e \"require('fs').copyFileSync('.clasp.dev.json','.clasp.json')\"",
    "use:prod": "node -e \"require('fs').copyFileSync('.clasp.prod.json','.clasp.json')\"",
    "pull": "npm run use:prod && clasp pull",
    "push": "npm run use:dev && clasp push",
    "watch": "npm run use:dev && clasp push --watch",
    "open": "npm run use:dev && clasp open",
    "open:prod": "npm run use:prod && clasp open",
    "deploy:dev": "npm run use:dev && clasp push && clasp deploy -i AKfycbziNYgamxGPl7B8OVB1YYb1bZ2VZEdb9RC59pTEtUzSOeVNxAZCVtMc-6jJrqmdk26XgQ",
    "release": "npm run use:prod && clasp push --force && clasp deploy -i AKfycby8gSa29N58Ny3mJjkDgdbnaIWUfQocPQwJ0QochAh_mLDsmYslJaO0ANDCbuXYNYV0",
    "deployments": "clasp deployments",
    "logs": "clasp logs",
    "fetch-data": "node scripts/fetch-sheet-data.js",
    "clear-data": "node scripts/clear-sheet-data.js",
    "clear-data:prod": "node scripts/clear-sheet-data.js --prod",
    "backlog:seed": "node scripts/seed-github-issues.mjs",
    "backlog:sync": "node scripts/sync-backlog.mjs"
  },
  "scriptsHelp": {
    "help": "show this command list",
    "test": "run the local test suite (no clasp, no live Sheet)",
    "login": "authenticate clasp with your Google account (one-time)",
    "use:dev | use:prod": "point .clasp.json at the DEV/PROD script (done automatically by the commands below)",
    "pull": "pull source from the PROD script editor (overwrites local files!)",
    "push": "push source to the DEV script editor/HEAD (never touches prod)",
    "watch": "like push, but re-pushes on every file save",
    "open | open:prod": "open the DEV/PROD script in the Apps Script editor",
    "deploy:dev": "push + update the DEV /exec deployment (needed by clear-data / fetch-data --dev)",
    "release": "push --force + redeploy the LIVE (PROD) web app — only from main, after tagging",
    "deployments": "list deployments of the currently selected script",
    "logs": "tail Apps Script logs of the currently selected script",
    "fetch-data": "snapshot PROD sheets to data/ (gitignored); add -- --dev for the DEV sheet",
    "clear-data": "DEV: wipe transactional sheets (Trips, Outlets, waybills, logs)",
    "clear-data:prod": "PROD: same wipe, asks you to type PRODUCTION — post-release only",
    "backlog:seed": "seed GitHub issues from the backlog doc",
    "backlog:sync": "regenerate BACKLOG.md from the GitHub Project board"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
