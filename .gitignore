# Local-only backlog snapshot — source of truth is the GitHub Project.
# Regenerate on demand with `npm run backlog:sync` (see CLAUDE.md).
BACKLOG.md

# Session-local resume file written by Claude when blocked on human input (see CLAUDE.md)
HANDOFF.md

# Generated env pointer — written by `npm run use:dev` / `use:prod`.
# The committed sources are .clasp.prod.json and .clasp.dev.json.
.clasp.json

*.xlsx
*.pdf
node_modules/
.clasprc.json
.env
data/

# Wrangler local dev state (miniflare caches, bundle temp files)
.wrangler/
