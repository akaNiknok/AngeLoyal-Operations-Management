**/**
!*.gs
!*.html
!appsscript.json

# The GitHub Pages launcher (pages/index.html) is NOT an Apps Script partial —
# re-ignore it so `!*.html` above doesn't push it into the script project.
pages/**
