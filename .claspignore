**/**
!*.gs
!*.html
!appsscript.json

# The frontend is a static site on Cloudflare Pages, not Apps Script partials —
# re-ignore it so `!*.html` above doesn't push it into the script project.
web/**

# The v2 backend and its migrations are Cloudflare code, never Apps Script.
server/**
migrations/**
